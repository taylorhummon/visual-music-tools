import { AnimationOption } from "@scalesTool/utilities/clock"


// Default Settings
export const DEFAULT_IS_USING_DEGREE_SPOTLIGHT: boolean = false
export const DEFAULT_IS_USING_SOLFEGE: boolean = false
export const DEFAULT_IS_ANCHORING_ROOT: boolean = false
export const DEFAULT_IS_USING_ANIMATION: boolean = true
export const DEFAULT_ANIMATION_OPTION: AnimationOption = AnimationOption.Combo

// Default to C-Major
export const DEFAULT_ROOT: number = -2
export const DEFAULT_DEGREE: number = 0

// Default to ducks not being in a row
export const DEFAULT_ARE_DUCKS_IN_A_ROW: boolean = false

// Allow up to two sharps or two flats on each note in the scale
export const MAX_DEGREE: number = 14
export const MIN_DEGREE: number = -14
